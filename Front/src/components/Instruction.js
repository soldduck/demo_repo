import React from 'react';

import { Container, Typography } from '@material-ui/core';

const Instruction = () => {
    return(
        <Container>
            <Typography align='center'>Вот сюда нужно вставить инструкцию.</Typography>
        </Container>
    );
}

export default Instruction;